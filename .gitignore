/node_modules
/vendor
/desktop-mode.zip
/dist/
/tests/phpunit/.phpunit.result.cache

# Local working copy of imported source art / prototypes (e.g. the
# original Wapuu pet demo). Reference material, not shipped.
/.scratch/

# Vite build output under assets/js/ — regenerate locally with `npm run
# build`. Adding a new bundle in vite.config.js does not require updating
# this list. Hand-written JS files that should stay tracked are explicitly
# re-included below.
/assets/js/*.js
!/assets/js/admin-bar.js
!/assets/js/media-library-enhanced.js

.DS_Store

# Default output file of macOS/BSD `script(1)` — a terminal session
# recorder. Easy to commit by accident with `git add .`.
/typescript

# Playwright CLI
.playwright-cli/

# Phpactor's per-project config — Zed's PHP language server re-generates
# this on demand from sensible defaults, so tracking it just creates churn.
/.phpactor.json

# `.claude/` is gitignored in the user's global gitignore. Re-include the
# `skills/` symlink so Claude Code's skill loader (which only scans
# `.claude/skills/`, not `.agents/skills/`) can find the shared skill
# library checked into `.agents/skills/`. Everything else under `.claude/`
# (local settings, transient state) stays ignored.
!/.claude/
/.claude/*
!/.claude/skills
